services:
  - type: web
    name: sanction-backend
    runtime: node
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        fromPort: true
      - key: MONGODB_URI
        # IMPORTANT: Set this in Render dashboard Environment tab
        # Format: mongodb+srv://username:password@cluster.mongodb.net/sanction_payment?retryWrites=true&w=majority
        sync: false

  - type: static
    name: sanction-dashboard
    buildCommand: cd dashboard && npm install && npm run build
    staticPublishPath: dashboard/dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://sanction-backend.onrender.com
        sync: false
